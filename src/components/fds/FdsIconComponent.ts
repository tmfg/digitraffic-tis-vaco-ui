import * as React from 'react'
import { createComponent } from '@lit-labs/react'
import FdsIcon from '../../../coreui-components/src/fds-icon'

export const FdsIconComponent = createComponent({
  tagName: 'fds-icon',
  elementClass: FdsIcon,
  react: React
})
